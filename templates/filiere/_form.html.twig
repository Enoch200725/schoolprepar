{# templates/filiere/_form.html.twig #}
{{ form_start(form) }}
    <div class="mb-3">
        {{ form_label(form.nom) }}
        {{ form_widget(form.nom) }}
        {% if form.nom.vars.errors|length > 0 %}
            <div class="text-danger mt-1">
                {% for error in form.nom.vars.errors %}
                    <small>{{ error.message }}</small>
                {% endfor %}
            </div>
        {% endif %}
    </div>

    <div class="mb-3">
        {{ form_label(form.code) }}
        {{ form_widget(form.code) }}
        {% if form.code.vars.errors|length > 0 %}
            <div class="text-danger mt-1">
                {% for error in form.code.vars.errors %}
                    <small>{{ error.message }}</small>
                {% endfor %}
            </div>
        {% endif %}
    </div>

    <div class="mb-3">
        {{ form_label(form.credit) }}
        {{ form_widget(form.credit) }}
        {% if form.credit.vars.errors|length > 0 %}
            <div class="text-danger mt-1">
                {% for error in form.credit.vars.errors %}
                    <small>{{ error.message }}</small>
                {% endfor %}
            </div>
        {% endif %}
    </div>

    <div class="mb-3">
        {{ form_label(form.image) }}
        {{ form_widget(form.image) }}
        <small class="text-muted">Entrez une URL d'image (ex: https://example.com/image.jpg)</small>
    </div>

    {{ form_widget(form.submit) }}
{{ form_end(form) }}