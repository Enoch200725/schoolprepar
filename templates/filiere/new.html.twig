{# templates/filiere/new.html.twig #}
{% extends 'admin/base.html.twig' %}

{% block titre %}Nouvelle filière - Admin{% endblock %}
{% block page_title %}Ajouter une filière{% endblock %}

{% block contenuPrincipal %}
<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Formulaire d'ajout</h3>
            </div>
            <div class="card-body">
                {{ include('filiere/_form.html.twig') }}
            </div>
            <div class="card-footer">
                <a href="{{ path('app_filiere_index') }}" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Retour à la liste
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}