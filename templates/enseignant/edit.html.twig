{# templates/enseignant/edit.html.twig #}
{% extends 'admin/base.html.twig' %}

{% block titre %}Modifier un enseignant - Admin{% endblock %}
{% block page_title %}Modifier l'enseignant{% endblock %}

{% block contenuPrincipal %}
<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Formulaire de modification</h3>
            </div>
            <div class="card-body">
                {{ include('enseignant/_form.html.twig') }}
            </div>
            <div class="card-footer">
                <a href="{{ path('app_enseignant_index') }}" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Retour à la liste
                </a>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-header bg-danger">
                <h3 class="card-title text-white">Zone de suppression</h3>
            </div>
            <div class="card-body">
                {{ include('enseignant/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}